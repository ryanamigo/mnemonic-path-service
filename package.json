{
  "name": "cloudflare-workers-openapi",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "db:migrate:create": "wrangler d1 migrations create mnemonic_db",
    "db:migrate:local": "wrangler d1 migrations apply mnemonic_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply mnemonic_db --remote"
  },
  "dependencies": {
    "chanfana": "^2.6.3",
    "hono": "^4.6.20",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "22.13.0",
    "@types/service-worker-mock": "^2.0.4",
    "wrangler": "^4.56.0"
  }
}